<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('app.css') }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Stick+No+Bills:wght@600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Stick+No+Bills:wght@600&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Almarai:wght@300;400;700;800&family=Righteous&family=Stick+No+Bills:wght@600&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/435de54bdb.js" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/aef8cfca6e.js" crossorigin="anonymous"></script>

</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color:#2a593e">
        <div class="container-fluid limit">
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsingNavbar3">
                <span class="navbar-toggler-icon"></span>
            </button>
  
            <div class="navbar-collapse collapse w-100" id="collapsingNavbar3">
                <ul class="navbar-nav w-100 justify-content-center">
                    <li class="nav-item active">
                    {# logo #}
                        <a class="nav-link" href={{ path('app_home_page') }}>
                            <img src="{{ asset('logo2.png') }}" alt="logo2" style=" height: 30px; width: auto; margin-right: 30px;">
                        </a>
                    </li>
                    {% if user != null and is_granted('ROLE_ADMIN') %}
                    
                        <li class="nav-item">
                            <a class="nav-link" href={{ path('admin') }}><b>Page admin</b></a>
                        </li>
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link" href={{ path('app_home_page') }}>Accueil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href={{ path('app_blogs') }}>Articles</a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('app_posts') }}>Nos offres</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href={{ path('app_contact') }}>Nous contacter</a>
                    </li>
                </ul>

                <ul class="nav navbar-nav ms-auto w-100 justify-content-end">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {% if user != null %}
                                Connecté en tant que : <b>{{ user.username }}</b>
                            {% else %}
                                Non connecté
                            {% endif %}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarScrollingDropdown">
                            {% if user != null %}
                                <li><a class="dropdown-item" href={{ path('app_profile') }}>Mon compte</a></li>
                                <li><a class="dropdown-item" href={{ path('app_logout') }}>Se déconnecter</a></li>
                            {% else %}
                                <li><a class="dropdown-item" href={{ path('app_login') }}>Se connecter</a></li>
                                <li><a class="dropdown-item" href={{ path('app_register') }}>S'enregistrer</a></li>
                            {% endif %}
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div>
        {% block body %}{% endblock %}
    </div>
    {% block footer %}
    
        <footer class="footer">
            <div class="footer-content">
                <div class="logo">
                    <h2 class="logo">GLEENER</h2>
                </div>
            </div>
            <div class="footer-navbar">
                <ul class="nav-list">
                    <li class="nav-item-footer"><a href={{ path('app_blogs') }} title="home" class="nav-item-link-footer">Articles</a></li>
                    <li class="nav-item-footer"><a href={{ path('app_posts') }} title="about" class="nav-item-link-footer">Nos offres</a></li>
                    <li class="nav-item-footer"><a href={{ path('app_contact') }} title="Contacts" class="nav-item-link-footer">Nous contacter</a></li>
                </ul>
            </div>
            <div class="nav-licence">
                <p class="nav-licence-company">GLeener<p class="nav-licence-author">Mentions légales</p></p>
            </div>
        </footer>
    
    {% endblock %}

    <!-- jQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <!-- Popper.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.10.2/umd/popper.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/js/bootstrap.min.js"></script>